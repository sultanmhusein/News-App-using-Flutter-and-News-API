import 'package:flutter/material.dart';

class HeadlinesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Headlines', textScaleFactor: 2.0,),
    );
  }
}